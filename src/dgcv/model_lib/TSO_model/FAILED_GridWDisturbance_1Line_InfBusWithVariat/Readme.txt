This was a first attempt to implement a ramp in omega for the infinite bus.

Finally, we chose to use the InfiniteBusFromTable model because the InfiniteBusWithVariations model
used returns omega to the initial value after finishing the event.